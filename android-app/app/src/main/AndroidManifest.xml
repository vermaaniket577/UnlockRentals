<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Package visibility queries for Android 11+ (API 30+) for UPI Payment Apps -->
    <queries>
        <!-- UPI Schemes -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="upi" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tez" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="phonepe" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="paytmmp" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="paytm" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="bhim" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="credpay" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="gpay" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="ppe" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="mobikwik" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="amazonpay" />
        </intent>

        <!-- Popular UPI App Packages for Intent Resolution -->
        <package android:name="com.google.android.apps.nbu.paisa.user" /> <!-- Google Pay -->
        <package android:name="com.phonepe.app" /> <!-- PhonePe -->
        <package android:name="net.one97.paytm" /> <!-- Paytm -->
        <package android:name="in.org.npci.upiapp" /> <!-- BHIM -->
        <package android:name="com.dreamplug.androidapp" /> <!-- CRED -->
        <package android:name="com.whatsapp" /> <!-- WhatsApp Pay -->
        <package android:name="com.amazon.mShop.android.shopping" /> <!-- Amazon Pay -->
        <package android:name="in.amazon.mShop.android.shopping" /> <!-- Amazon Pay India -->
        <package android:name="com.csam.icici.bank.imobile" /> <!-- iMobile Pay -->
        <package android:name="com.msf.kbank.mobile" /> <!-- Kotak 811 -->
        <package android:name="com.axis.mobile" /> <!-- Axis Mobile -->
        <package android:name="com.sbi.upi" /> <!-- BHIM SBI Pay -->
        <package android:name="com.sbi.lotusintouch" /> <!-- YONO SBI -->
        <package android:name="com.hdfcbank.payzapp" /> <!-- PayZapp -->
        <package android:name="com.mobikwik_new" /> <!-- MobiKwik -->
        <package android:name="com.freecharge.android" /> <!-- FreeCharge -->
        <package android:name="com.navi.personal.loan.finance" /> <!-- Navi -->
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.UnlockRentals"
        android:usesCleartextTraffic="true"
        android:hardwareAccelerated="true">

        <!-- Main WebView Activity (Direct Zero-Delay Launcher with Starting Theme) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.UnlockRentals.Splash"
            android:configChanges="orientation|screenSize|keyboardHidden|layoutDirection|screenLayout|smallestScreenSize"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Custom App Scheme for Instant Social Auth Token Handoff -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="unlockrentals" android:host="auth" />
            </intent-filter>

            <!-- Deep Links for the website domain -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                    android:host="unlockrentals.com" />
                <data android:scheme="https"
                    android:host="www.unlockrentals.com" />
            </intent-filter>
        </activity>

        <!-- Forwarding Activity for backwards compatibility -->
        <activity
            android:name=".SplashActivity"
            android:exported="false"
            android:theme="@style/Theme.UnlockRentals.Splash"
            android:screenOrientation="portrait" />

    </application>

</manifest>
